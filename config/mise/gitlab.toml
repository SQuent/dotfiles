# Git identity — GitLab (personal)
[env]
GIT_SSH_COMMAND = "ssh -i ~/.ssh/gitlab_id_rsa -F /dev/null"
GIT_AUTHOR_NAME = "{{env.USER_GIT_GITLAB}}"
GIT_AUTHOR_EMAIL = "{{env.EMAIL_GIT_GITLAB}}"
GIT_COMMITTER_NAME = "{{env.USER_GIT_GITLAB}}"
GIT_COMMITTER_EMAIL = "{{env.EMAIL_GIT_GITLAB}}"