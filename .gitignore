install.log

# `nix build`/`home-manager build` without --no-link drop this symlink here.
result

# Local-only: corporate proxy CA testing (see Dockerfile.test), never committed.
Dockerfile.test
docker/
